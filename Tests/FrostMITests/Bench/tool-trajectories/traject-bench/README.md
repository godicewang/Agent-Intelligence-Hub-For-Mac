# TRAJECT-Bench Adapter

This area is reserved for tool-trajectory normalization. The first FrostMI adapter should convert upstream tool definitions and ordered/parallel calls into FrostMI `ToolDiscovery`, `ToolCall`, `ToolArguments`, and dependency-edge events.
